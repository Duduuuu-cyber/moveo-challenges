environment: Production-Kubernetes
kafka:
  broker: kafka-service:9092
  topic: moveo-challenge-topic

frontend:
  image: devops-frontend:latest
  replicaCount: 2
  port: 3000

producer:
  image: devops-producer:latest
  replicaCount: 1

consumer:
  image: devops-consumer:latest
  replicaCount: 1
