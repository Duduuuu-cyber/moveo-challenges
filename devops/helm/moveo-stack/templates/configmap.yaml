apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  KAFKA_BROKER: {{ .Values.kafka.broker | quote }}
  TOPIC_NAME: {{ .Values.kafka.topic | quote }}
  ENV_NAME: {{ .Values.environment | quote }}
  PORT: {{ .Values.frontend.port | quote }}
