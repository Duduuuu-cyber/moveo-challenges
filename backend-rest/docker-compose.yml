version: '3.8'

services:
  task-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: task-rest-api
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      JWT_SECRET: "moveo-ai-secure-jwt-secret-key-2026"
    restart: always
